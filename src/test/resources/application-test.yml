spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  agent:
    worker:
      scheduling-enabled: false
      retry-delay-ms: 0
  git:
    worker:
      retry-delay-ms: 0
  code-context:
    worker:
      retry-delay-ms: 0
  ci:
    worker:
      poll-delay-ms: 0
      retry-delay-ms: 0
